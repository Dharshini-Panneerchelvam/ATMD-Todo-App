import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  emptyMessage: {
    textAlign: "center",
    marginTop: 20,
    fontStyle: "italic",
    color: "#999",
  },
});

export default styles;
